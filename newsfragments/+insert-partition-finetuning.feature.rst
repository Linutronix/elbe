Add an ``<insert_partition>`` project-finetuning action, the inverse of
``<extract_partition>``, to inject a file (e.g. dm-verity hash data
generated in an earlier step) into a partition of the target image.
